<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itCat.crmEX.settings.mapper.UserMapper">
    <select id="selectUserByUsernameAndPassword" parameterType="java.util.Map" resultType="com.itCat.crmEX.settings.domain.User">
        select id, username, name, password, email, phone, expireTime, lockStatus, allowIps, createBy, createTime, editBy, editTime
        from `tbl_user`
        where username = #{username} and password = #{password}
    </select>
</mapper>